// Defines a binary tree datatype.
// - A: The type of values stored in the tree.
// Constructors:
// - node: Represents an internal node with left and right subtrees.
// - leaf: Represents a leaf node with a value.
Tree
: ∀(A: *)
  *
= λA #[]{
  #node{ left: (Tree A) right: (Tree A) } : (Tree A)
  #leaf{ value: A } : (Tree A)
}