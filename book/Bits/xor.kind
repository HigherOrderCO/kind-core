// Performs bitwise XOR operation on two Bits sequences.
// - a: The first Bits sequence.
// - b: The second Bits sequence.
// = The result of bitwise XOR operation.
Bits/xor
: ∀(a: Bits)
  ∀(b: Bits)
  Bits
= λ{
  #e: λb
    #e{}
  #i: λa.pred λ{
    #e: #e{}
    #i: λb.pred #i{(Bits/xor a.pred b.pred)}
    #o: λb.pred #o{(Bits/xor a.pred b.pred)}
  }
  #o: λa.pred λ{
    #e: #e{}
    #i: λb.pred #o{(Bits/xor a.pred b.pred)}
    #o: λb.pred #i{(Bits/xor a.pred b.pred)}
  }
}