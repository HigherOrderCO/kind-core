// A simple function to demonstrate the use of Kind

main
: (Map (Maybe U32))
= 
	let leaf = (Map/leaf (Maybe U32))
	let zero = (Map/set U32 leaf 0 0)
	let um = (Map/set U32 zero 1 10)
	let dois = (Map/set U32 um 2 20)

	dois
	