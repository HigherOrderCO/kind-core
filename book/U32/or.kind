// Performs bitwise OR operation on two U32 values.
// - a: The first U32 value.
// - b: The second U32 value.
// = The result of bitwise OR operation between `a` and `b`.
U32/or
: ∀(a: U32)
  ∀(b: U32)
  U32
= λa λb
  (| a b)