// Checks if one U32 is less than another.
// - a: The first U32 value.
// - b: The second U32 value.
// = True if `a` is less than `b`, False otherwise.
U32/lt
: ∀(a: U32)
  ∀(b: U32)
  Bool
= λa λb
  (Bool/not (U32/gte a b))