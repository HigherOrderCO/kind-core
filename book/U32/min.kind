// Returns the minimum of two U32 numbers.
// - a: The first U32 number.
// - b: The second U32 number.
// = The min of the two input numbers.
U32/min
: ∀(a: U32)
  ∀(b: U32)
  U32
  = λa λb
(Bool/if U32 (U32/lte a b) a b)

