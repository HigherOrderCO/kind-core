// Returns the maximum of two U32 numbers.
// - a: The first U32 number.
// - b: The second U32 number.
// = The larger of the two input numbers.
U32/max
: ∀(a: U32)
  ∀(b: U32)
  U32
= λa λb
  (Bool/if U32 (U32/gte a b) a b)

  
