// Mirrors an equality proof, reversing the direction of equality.
// - A: The type of the values being compared.
// - a: The first value.
// - b: The second value.
// - e: Proof that `a` equals `b`.
// = A proof that `b` equals `a`.
Equal/mirror
: ∀(A: *)
  ∀(a: A)
  ∀(b: A)
  ∀(e: (Equal A a b))
  (Equal A b a)
= λA λa λb λ{
  #refl: #refl{}
}