// Checks if the first number is greater than the second
// - a: The first natural number
// - b: The second natural number
// = True if a is greater than b, False otherwise
Nat/is_gtn
: ∀(a: Nat)
  ∀(b: Nat)
  Bool
= λ{
  #zero: λb #false{}
  #succ: λa.pred λ{
    #zero: #true{}
    #succ: λb.pred (Nat/is_gtn a.pred b.pred)
  }
}
