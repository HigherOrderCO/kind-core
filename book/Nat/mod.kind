// Computes the modulo (remainder) of two natural numbers.
// - a: The dividend (number to be divided).
// - b: The divisor (number to divide by).
// = The remainder of a divided by b.
//   If b is zero, returns zero to avoid division by zero.
Nat/mod
: ∀(a: Nat)
  ∀(b: Nat)
  Nat
= λa λb
  (Bool/if
    Nat
    (Nat/is_zero b)
    #z{}
    (Nat/sub a (Nat/mul (Nat/div a b) b)))