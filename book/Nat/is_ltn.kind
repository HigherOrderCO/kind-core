// Checks if the first number is less than the second
// - a: The first natural number
// - b: The second natural number
// = True if a is less than b, False otherwise
Nat/is_ltn
: ∀(a: Nat)
  ∀(b: Nat)
  Bool
= λ{
  #z: λ{
    #z: #f{}
    #s: λb.pred #t{}
  }
  #s: λa.pred λ{
    #z: #f{}
    #s: λb.pred (Nat/is_ltn a.pred b.pred)
  }
}
