// Checks if a map is a leaf (empty)
// - A: The type of values stored in the map
// - map: The input map
// = True if the map is a leaf, False otherwise
Map/is_leaf
: ∀(A: *)
  ∀(map: (Map A))
  Bool
= λA λ{
  #leaf: #t{}
  #node: λvalue λleft λright #f{}
}