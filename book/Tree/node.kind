// Constructs an internal node of a binary tree.
// - A: The type of values stored in the tree.
// - left: The left subtree.
// - right: The right subtree.
// = A new tree node with the given left and right subtrees.
Tree/node
: ∀(A: *)
  ∀(left: (Tree A))
  ∀(right: (Tree A))
  (Tree A)
= λA λleft λright
  #node{left right}