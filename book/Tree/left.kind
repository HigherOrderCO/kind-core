// Gets the left subtree of a node.
// - A: The type of values in the tree.
// - tree: The tree to get the left subtree from.
// = The left subtree if the tree is a node, or the tree itself if it's a leaf.
Tree/left
: ∀(A: *)
  ∀(tree: (Tree A))
  (Tree A)
= λA λ{
  #node: λleft λright
    left
  #leaf: λvalue
    (Tree/leaf A value)
}